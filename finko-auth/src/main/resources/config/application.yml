
server:
  port: 8080 # 项目启动的端口

spring:
  profiles:
    active: dev # 默认激活 dev 本地开发环境
  config:
    import: mybatis-plus-generator.yml
  application:
    name: finko-auth


mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
#  type-aliases-package: com.fincodehub.finko.auth.mapper
